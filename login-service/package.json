{
  "name": "notebooklm-login-service",
  "version": "0.2.0",
  "private": true,
  "description": "Companion service: creates a remote Browserbase browser session for NotebookLM login (embeddable live-view iframe, works on iPad Safari), captures the resulting session cookie, and hands it to the Supabase-backed bridge for Vault storage. Only runs during an explicit user-initiated 'Connect NotebookLM' action.",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
